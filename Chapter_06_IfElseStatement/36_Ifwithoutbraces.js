let x = 10;

if (x > 5)

    console.log("x is big");
/**
This prints "x is big" because x > 5 is true. 
If you later add a second line under it, that second line would run every time (it is not part of the if), which is a classic bug.

 */